diff --git a/vis-clipboard b/vis-clipboard
index 8397ee2..0584e59 100755
--- a/vis-clipboard
+++ b/vis-clipboard
@@ -13,14 +13,14 @@ vc_usage() {
 
 vc_determine_command() {
 	if [ -n "$WAYLAND_DISPLAY" ]; then
-		for c in wl-copy wl-paste; do
+		for c in waycopy waypaste; do
 			if type "$c" >/dev/null 2>&1; then
-				echo "wlclipboard"
+				echo "wayclip"
 				return 0
 			fi
 		done
 	fi
-	
+
 	if [ -n "$DISPLAY" ]; then
 		for c in xclip xsel; do
 			if type "$c" >/dev/null 2>&1; then
@@ -79,20 +79,12 @@ vc_paste() {
 	exit $?
 }
 
-vc_wlclipboard_copy() {
-	if [ "$sel" = "primary" ]; then
-		wl-copy --primary -t TEXT
-	else
-		wl-copy -t TEXT
-	fi
+vc_wayclip_copy() {
+	waycopy -t TEXT
 }
 
-vc_wlclipboard_paste() {
-	if [ "$sel" = "primary" ]; then
-		wl-paste --primary -t text
-	else
-		wl-paste -t text
-	fi
+vc_wayclip_paste() {
+	waypaste -t text
 }
 
 vc_xsel_copy() {
